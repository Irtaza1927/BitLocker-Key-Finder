# BitLocker Key Finder - Dependencies
# FAST Islamabad | Digital Forensics | Semester 6

# Core (always needed)
chardet>=5.0.0

# Office document parsing
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
striprtf>=0.0.26

# PDF parsing (install both — tool tries pdfplumber first, falls back to PyPDF2)
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Data (optional fallback for xlsx)
pandas>=2.0.0

# PDF report generation
reportlab>=4.0.0